version: '3.1'
services:
  db:
    container_name: postgres
    image: postgres
    ports:
      - 5432:5432
    restart: always
    volumes:
      - $PWD/postgres/data:/var/lib/postgresql/data
    env_file:
      - $PWD/postgres/postgres.env
